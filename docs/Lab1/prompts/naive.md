# Prompt: Naive (Spec-light)

Build a simple web app to capture and triage customer feature requests. Include a form to submit requests with fields for title, description, customer segment, impact, priority, status, and tags, plus a list view to see submissions with sorting and filtering.

Provide basic styling, a README with setup/run/test instructions, and minimal tests. Use any lightweight stack that runs locally with npm/yarn.
